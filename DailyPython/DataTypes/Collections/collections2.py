# lists

movies = ['superman', 'spiderman', 'ant man']
print(movies[-1])
# last_element = movies[-1]

last_element_index = len(movies) - 1
print(last_element_index)
print(len(movies))
# len()

# indexes collections from 0
