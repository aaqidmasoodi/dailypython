import numpy as np

arr = np.random.randint(2,6, size=(15,15)) # used more often

arr2 = np.random.rand(2,2) # you give it the shape as the parameter

# can you code a rock paper sissor
print(arr2*100) # __add__ __mul__ __sub__

print(arr)

iarr = np.identity(3) # shape? square matrix

print(iarr)